using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StoryQueue : MonoBehaviour
{
/*    public AudioClip enLitenHandling;
    public AudioClip ljudOchVarme;
    public AudioClip medLiteKarlek;
    public AudioClip stillaNagonsHunger;
    public AudioClip takOverHuvudet;

    public List<AudioClip> audioQueue = new List<AudioClip>();
    public AudioSource storyAudioSource;

    int index = 0;

    private void Start()
    {
        audioQueue.Add(enLitenHandling);
        audioQueue.Add(ljudOchVarme);
        audioQueue.Add(medLiteKarlek);
        audioQueue.Add(stillaNagonsHunger);
        audioQueue.Add(takOverHuvudet);
    }
    public void StorySound()
    {
        storyAudioSource.clip = audioQueue[index];
        storyAudioSource.Play();
        index++;
    }*/
}
